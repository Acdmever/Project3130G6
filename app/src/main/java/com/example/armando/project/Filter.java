package com.example.armando.project;

public class Filter {

    public boolean filterByClassLevel(int level) {
        // Querying database to filter class by class level
        // Updating display
        return true;
    }

    public boolean filterByDepartment(String department) {
        // Querying database to filter class by department level
        // Updating course table
        return true;
    }
}
